---
title: 'Update Booking'
api: 'PUT https://your-site.com/wp-json/latepoint-api/v1/bookings/{id}'
description: 'Updates an existing booking'
---

## Description

This endpoint allows you to update an existing booking. You can modify any booking field, including schedules, status, customer information, and notes.

## Authentication

<ParamField header="X-API-Key" type="string" required>
  Your LatePoint API Key with write permissions
</ParamField>

## Path Parameters

<ParamField path="id" type="integer" required>
  Unique ID of the booking to update
</ParamField>

## Request Body

### Booking Fields

<ParamField body="service_id" type="integer">
  Service ID (requires availability verification if changed)
</ParamField>

<ParamField body="agent_id" type="integer">
  Agent ID (requires availability verification if changed)
</ParamField>

<ParamField body="location_id" type="integer">
  Location ID
</ParamField>

<ParamField body="start_date" type="string">
  New booking date (format: YYYY-MM-DD)
</ParamField>

<ParamField body="start_time" type="string">
  New start time (format: HH:MM in 24-hour format)
</ParamField>

<ParamField body="duration" type="integer">
  Custom duration in minutes
</ParamField>

<ParamField body="status" type="string">
  Booking status
  
  **Possible values:**
  - `pending` - Pending
  - `confirmed` - Confirmed
  - `cancelled` - Cancelled
  - `completed` - Completed
  - `no_show` - No show
</ParamField>

### Customer Information

<ParamField body="customer_id" type="integer">
  Change to a different customer (customer ID)
</ParamField>

<ParamField body="customer.first_name" type="string">
  Update customer first name
</ParamField>

<ParamField body="customer.last_name" type="string">
  Update customer last name
</ParamField>

<ParamField body="customer.email" type="string">
  Update customer email
</ParamField>

<ParamField body="customer.phone" type="string">
  Update customer phone
</ParamField>

### Additional Fields

<ParamField body="price" type="string">
  Custom price for this booking
</ParamField>

<ParamField body="notes" type="string">
  Public booking notes
</ParamField>

<ParamField body="internal_notes" type="string">
  Internal notes (only visible to staff)
</ParamField>

<ParamField body="payment_status" type="string">
  Payment status
  
  **Possible values:**
  - `not_paid` - Not paid
  - `partially_paid` - Partially paid
  - `paid` - Paid
  - `refunded` - Refunded
</ParamField>

<ParamField body="payment_method" type="string">
  Payment method used
</ParamField>

<ParamField body="send_notification" type="boolean" default="false">
  Whether to send notification to customer about changes
</ParamField>

<ParamField body="custom_fields" type="object">
  Custom booking fields
</ParamField>

## Response

### Successful Response (200 OK)

<ResponseField name="status" type="string">
  Response status ("success")
</ResponseField>

<ResponseField name="data" type="object">
  Updated booking object with all fields
</ResponseField>

<ResponseField name="changes" type="object">
  Summary of changes made
  
  <Expandable title="Change Information">
    <ResponseField name="modified_fields" type="array">
      List of fields that were modified
    </ResponseField>
    
    <ResponseField name="previous_values" type="object">
      Previous values of modified fields
    </ResponseField>
    
    <ResponseField name="notification_sent" type="boolean">
      Whether notification was sent to customer
    </ResponseField>
  </Expandable>
</ResponseField>

## Examples

### Change Booking Status

<CodeGroup>

```bash cURL
curl -X PUT "https://your-site.com/wp-json/latepoint-api/v1/bookings/123" \
  -H "X-API-Key: lp_live_1234567890abcdef" \
  -H "Content-Type: application/json" \
  -d '{
    "status": "confirmed",
    "send_notification": true
  }'
```

```javascript JavaScript
const bookingId = 123;
const updateData = {
  status: 'confirmed',
  send_notification: true
};

const response = await fetch(`https://your-site.com/wp-json/latepoint-api/v1/bookings/${bookingId}`, {
  method: 'PUT',
  headers: {
    'X-API-Key': 'lp_live_1234567890abcdef',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify(updateData)
});

const result = await response.json();
console.log(result);
```

```python Python
import requests
import json

booking_id = 123
update_data = {
    'status': 'confirmed',
    'send_notification': True
}

response = requests.put(
    f'https://your-site.com/wp-json/latepoint-api/v1/bookings/{booking_id}',
    headers={
        'X-API-Key': 'lp_live_1234567890abcdef',
        'Content-Type': 'application/json'
    },
    data=json.dumps(update_data)
)

result = response.json()
print(result)
```

```php PHP
<?php
$booking_id = 123;
$update_data = [
    'status' => 'confirmed',
    'send_notification' => true
];

$curl = curl_init();

curl_setopt_array($curl, [
    CURLOPT_URL => "https://your-site.com/wp-json/latepoint-api/v1/bookings/{$booking_id}",
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_CUSTOMREQUEST => 'PUT',
    CURLOPT_POSTFIELDS => json_encode($update_data),
    CURLOPT_HTTPHEADER => [
        'X-API-Key: lp_live_1234567890abcdef',
        'Content-Type: application/json'
    ]
]);

$response = curl_exec($curl);
$result = json_decode($response, true);

curl_close($curl);
print_r($result);
?>
```

</CodeGroup>

### Reschedule Booking

<CodeGroup>

```bash cURL
curl -X PUT "https://your-site.com/wp-json/latepoint-api/v1/bookings/123" \
  -H "X-API-Key: lp_live_1234567890abcdef" \
  -H "Content-Type: application/json" \
  -d '{
    "start_date": "2024-01-25",
    "start_time": "16:00",
    "notes": "Rescheduled by customer request",
    "send_notification": true
  }'
```

```javascript JavaScript
const rescheduleData = {
  start_date: '2024-01-25',
  start_time: '16:00',
  notes: 'Rescheduled by customer request',
  send_notification: true
};

const response = await fetch(`https://your-site.com/wp-json/latepoint-api/v1/bookings/123`, {
  method: 'PUT',
  headers: {
    'X-API-Key': 'lp_live_1234567890abcdef',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify(rescheduleData)
});

const result = await response.json();
```

```python Python
reschedule_data = {
    'start_date': '2024-01-25',
    'start_time': '16:00',
    'notes': 'Rescheduled by customer request',
    'send_notification': True
}

response = requests.put(
    'https://your-site.com/wp-json/latepoint-api/v1/bookings/123',
    headers={
        'X-API-Key': 'lp_live_1234567890abcdef',
        'Content-Type': 'application/json'
    },
    data=json.dumps(reschedule_data)
)

result = response.json()
```

</CodeGroup>

### Update Customer Information

<CodeGroup>

```bash cURL
curl -X PUT "https://your-site.com/wp-json/latepoint-api/v1/bookings/123" \
  -H "X-API-Key: lp_live_1234567890abcdef" \
  -H "Content-Type: application/json" \
  -d '{
    "customer": {
      "phone": "+52 555 999 8888",
      "email": "new.email@example.com"
    },
    "custom_fields": {
      "emergency_contact": "Maria Perez - +52 555 777 6666"
    }
  }'
```

```javascript JavaScript
const customerUpdateData = {
  customer: {
    phone: '+52 555 999 8888',
    email: 'new.email@example.com'
  },
  custom_fields: {
    emergency_contact: 'Maria Perez - +52 555 777 6666'
  }
};

const response = await fetch(`https://your-site.com/wp-json/latepoint-api/v1/bookings/123`, {
  method: 'PUT',
  headers: {
    'X-API-Key': 'lp_live_1234567890abcdef',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify(customerUpdateData)
});
```

</CodeGroup>

## Example Response

### Successful Update

```json
{
  "status": "success",
  "data": {
    "id": 123,
    "booking_code": "LP-2024-001",
    "status": "confirmed",
    "start_date": "2024-01-25",
    "start_time": "16:00",
    "end_time": "17:00",
    "duration": 60,
    "price": "75.00",
    "customer": {
      "id": 45,
      "first_name": "John",
    "last_name": "Doe",
    "email": "new.email@example.com",
      "phone": "+52 555 999 8888"
    },
    "agent": {
      "id": 3,
      "display_name": "Dr. Maria Garcia",
    "email": "maria.garcia@clinic.com"
    },
    "service": {
      "id": 7,
      "name": "General Consultation",
      "duration": 60,
      "price": "75.00"
    },
    "location": {
      "id": 1,
      "name": "Main Clinic",
    "address": "123 Main St, City"
    },
    "notes": "Rescheduled by customer request",
    "updated_at": "2024-01-20T14:30:00Z"
  },
  "changes": {
    "modified_fields": [
      "start_date",
      "start_time",
      "end_time",
      "status",
      "notes",
      "customer.email",
      "customer.phone"
    ],
    "previous_values": {
      "start_date": "2024-01-15",
      "start_time": "14:30",
      "end_time": "15:30",
      "status": "pending",
      "notes": "Patient's first consultation",
      "customer.email": "john.doe@email.com",
      "customer.phone": "+52 555 123 4567"
    },
    "notification_sent": true
  }
}
```

## Error Codes

<ResponseExample>

```json Error 404 - Booking Not Found
{
  "status": "error",
  "error": {
    "code": "booking_not_found",
    "message": "Booking with ID 123 was not found"
  }
}
```

```json Error 409 - Schedule Conflict
{
  "status": "error",
  "error": {
    "code": "time_slot_unavailable",
    "message": "The new time slot is not available",
    "details": {
      "requested_time": "2024-01-25 16:00",
      "agent_id": 3,
      "conflicting_booking_id": 156,
      "available_slots": [
        "2024-01-25 17:00",
        "2024-01-25 18:00"
      ]
    }
  }
}
```

```json Error 422 - Validation Failed
{
  "status": "error",
  "error": {
    "code": "validation_failed",
    "message": "The provided data failed validation",
    "details": {
      "start_date": "Date cannot be in the past",
      "customer.email": "Invalid email",
      "status": "Invalid status for this booking"
    }
  }
}
```

```json Error 400 - Invalid Data
{
  "status": "error",
  "error": {
    "code": "invalid_request",
    "message": "Invalid input data",
    "details": {
      "service_id": "The specified service does not exist",
      "agent_id": "The specified agent does not exist"
    }
  }
}
```

</ResponseExample>

## Validations

### Automatic Validations

1. **Availability**: If you change date, time or agent, availability is verified
2. **Valid States**: Only allows valid status transitions
3. **Dates**: Does not allow changing to past dates
4. **Unique Email**: Verifies that email is not in use by another customer
5. **Permissions**: Verifies that you have permissions to modify the booking

### Status Transition Rules

| Current Status | Allowed States |
|---------------|--------------------|
| `pending` | `confirmed`, `cancelled` |
| `confirmed` | `completed`, `cancelled`, `no_show` |
| `cancelled` | `pending`, `confirmed` |
| `completed` | `cancelled` (special permissions only) |
| `no_show` | `confirmed`, `cancelled` |

## Common Use Cases

### 1. Confirm Pending Booking

```javascript
// Confirm a booking that was pending
async function confirmBooking(bookingId) {
  const response = await fetch(`/wp-json/latepoint-api/v1/bookings/${bookingId}`, {
    method: 'PUT',
    headers: {
      'X-API-Key': 'your_api_key',
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      status: 'confirmed',
      send_notification: true
    })
  });
  
  if (response.ok) {
    const result = await response.json();
    console.log('Booking confirmed:', result.data.booking_code);
    return result.data;
  }
  
  throw new Error('Error confirming booking');
}
```

### 2. Reschedule with Availability Check

```javascript
// Reschedule a booking by checking availability first
async function rescheduleBooking(bookingId, newDate, newTime, agentId, serviceId) {
  // First check availability
  const availabilityResponse = await fetch(
    `/wp-json/latepoint-api/v1/availability?service_id=${serviceId}&agent_id=${agentId}&date=${newDate}`
  );
  
  const availability = await availabilityResponse.json();
  
  if (!availability.data.available_slots.includes(newTime)) {
    throw new Error('The requested time slot is not available');
  }
  
  // Proceed with rescheduling
  const response = await fetch(`/wp-json/latepoint-api/v1/bookings/${bookingId}`, {
    method: 'PUT',
    headers: {
      'X-API-Key': 'your_api_key',
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      start_date: newDate,
      start_time: newTime,
      send_notification: true,
      notes: 'Rescheduled by customer request'
    })
  });
  
  return await response.json();
}
```

### 3. Bulk Status Update

```javascript
// Mark multiple bookings as completed
async function markBookingsCompleted(bookingIds) {
  const results = [];
  
  for (const bookingId of bookingIds) {
    try {
      const response = await fetch(`/wp-json/latepoint-api/v1/bookings/${bookingId}`, {
        method: 'PUT',
        headers: {
          'X-API-Key': 'your_api_key',
          'Content-Type': 'application/json'
        },
        body: JSON.stringify({
          status: 'completed',
          internal_notes: 'Marked as completed automatically'
        })
      });
      
      if (response.ok) {
        const result = await response.json();
        results.push({ id: bookingId, success: true, data: result.data });
      } else {
        results.push({ id: bookingId, success: false, error: await response.json() });
      }
    } catch (error) {
      results.push({ id: bookingId, success: false, error: error.message });
    }
  }
  
  return results;
}
```

### 4. Update Payment Information

```javascript
// Update payment status of a booking
async function updatePaymentStatus(bookingId, paymentStatus, paymentMethod = null) {
  const updateData = {
    payment_status: paymentStatus
  };
  
  if (paymentMethod) {
    updateData.payment_method = paymentMethod;
  }
  
  const response = await fetch(`/wp-json/latepoint-api/v1/bookings/${bookingId}`, {
    method: 'PUT',
    headers: {
      'X-API-Key': 'your_api_key',
      'Content-Type': 'application/json'
    },
    body: JSON.stringify(updateData)
  });
  
  return await response.json();
}
```

## Important Notes

<Note>
  **Partial Update**: You only need to send the fields you want to update. Fields not included will maintain their current values.
</Note>

<Warning>
  **Availability Check**: Always verify availability before changing date, time or agent to avoid conflicts.
</Warning>

<Tip>
  **Notifications**: Use `send_notification: true` to automatically inform the customer about important changes to their booking.
</Tip>

## Best Practices

### 1. Prior Verification

```javascript
// Always get current data before updating
const currentBooking = await fetch(`/wp-json/latepoint-api/v1/bookings/${bookingId}`);
const bookingData = await currentBooking.json();

// Then perform the update with new values
```

### 2. Error Handling

```javascript
// Implement robust error handling
try {
  const response = await fetch(`/wp-json/latepoint-api/v1/bookings/${bookingId}`, {
    method: 'PUT',
    body: JSON.stringify(updateData)
  });
  
  if (!response.ok) {
    const error = await response.json();
    
    if (error.error.code === 'time_slot_unavailable') {
      // Show alternative time slots
      console.log('Available slots:', error.error.details.available_slots);
    }
    
    throw new Error(error.error.message);
  }
  
  const result = await response.json();
  return result.data;
} catch (error) {
  console.error('Error updating booking:', error.message);
  throw error;
}
```

### 3. Change Logging

```javascript
// Use change information for auditing
const result = await updateBooking(bookingId, updateData);

if (result.changes.modified_fields.length > 0) {
  console.log('Modified fields:', result.changes.modified_fields);
  console.log('Previous values:', result.changes.previous_values);
  
  // Log to audit system
  logBookingChanges(bookingId, result.changes);
}
```